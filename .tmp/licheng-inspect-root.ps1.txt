$root='D:\rag-client-v1.0.0-windows-x64'; $out=@(); foreach($rel in @('state','versions','downloads','logs','client.bundle.cjs','client-launcher.cjs')){ $p=Join-Path $root $rel; if(Test-Path $p){ $item=Get-Item $p; $type = if($item.PSIsContainer){'dir'}else{'file'}; $out += [pscustomobject]@{ Path=$item.FullName; Type=$type } } }; $clientVersions=Join-Path $root 'versions\client'; if(Test-Path $clientVersions){ Get-ChildItem -Path $clientVersions -Force -ErrorAction SilentlyContinue | ForEach-Object { $type = if($_.PSIsContainer){'dir'}else{'file'}; $out += [pscustomobject]@{ Path=$_.FullName; Type=$type } } }; $out | ConvertTo-Json -Depth 4